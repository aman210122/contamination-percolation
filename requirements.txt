# Pinned dependency versions for the contamination-percolation experiments.
# Versions match the Dependency Versions appendix in the paper.
numpy==1.26.4
pandas==2.2.2
scipy==1.13.1
pyyaml==6.0.2
requests==2.32.3
openai==1.40.0
anthropic==0.34.0
databricks-sdk==0.30.0
google-genai==0.3.0
psycopg2-binary==2.9.9
# matplotlib is needed by figures_from_paper.py. It was not pinned in the
# paper, so set it to the version you used.
matplotlib
